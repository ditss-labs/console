{
  "name": "bot-wa-console",
  "version": "1.0.0",
  "description": "Realtime console for WhatsApp bot with MongoDB",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "mongodb": "^5.0.0"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  }
}
